# 企业级智能知识库系统需求文档（自学路线 + 可落地版）

> **文档定位**：面向大模型应用开发初学者，提供可渐进实现的学习路径；同时满足**企业内部试点 / 生产落地**所需的功能边界、安全与运维要求。  
> **版本**：2.0  
> **适用范围**：单团队或部门级知识库（可先单机/小集群部署），后续可演进为多租户 SaaS。

---

## 1. 项目背景

### 1.1 项目目标

构建基于 **RAG（Retrieval-Augmented Generation）** 的企业知识库问答系统，实现：

- 企业文档的安全导入、解析、索引与检索；
- 带**可追溯引用**的自然语言问答与多轮对话；
- 可观测、可配置、可部署的**工程化交付**，而非仅演示原型。

### 1.2 核心价值

| 维度 | 说明 |
| ---- | ---- |
| 数据安全 | 支持私有化部署；密钥与敏感配置不入库明文；传输 TLS |
| 知识激活 | 制度、手册、技术文档转化为可检索、可问答的知识服务 |
| 引用可信 | 答案绑定 chunk 与元数据（文档名、页码/章节），降低幻觉风险 |
| 低门槛 | 模块化、分阶段交付；文档与接口契约清晰，便于二次开发 |

### 1.3 典型应用场景

- 人事/财务/合规类制度问答  
- 产品与技术文档检索与答疑  
- 运维与排障知识库  
- 项目交付物沉淀与复用  

### 1.4 读者与交付物

| 角色 | 关注点 |
| ---- | ------ |
| 初学者 | 学习路径、MVP 范围、模块边界 |
| 项目负责人 | 里程碑、验收指标、风险与依赖 |
| 运维 / 安全 | 部署拓扑、密钥、日志审计、备份 |
| 业务方 | 问答质量、引用、响应时间 |

**交付物建议**：源代码、Docker Compose（或 K8s 清单草案）、API 文档（OpenAPI）、部署与运维说明、最小评测集与基线指标。

---

## 2. 范围与非目标

### 2.1 本期范围内（建议分阶段，见第 12 章）

- 单组织内的知识库空间（可先不做完整多租户计费）。
- 文本为主文档（PDF / DOCX / TXT / MD / XLSX）；图片内文字可作**扩展阶段**（OCR）。
- 同步或异步文档入库流水线、检索、流式问答、引用展示、基础管理后台或配置页。

### 2.2 非目标（避免范围失控）

- 通用互联网搜索型 Agent（无边界工具调用）默认不纳入首期必达。  
- 不承诺「100% 准确」；以**可引用、可拒答、可评测**为工程目标。  
- 复杂协同编辑、在线 Office 编辑、全文档版本对比等 CMS 能力不属 RAG 核心，可后续对接。

---

## 3. 角色与权限（企业落地必备）

### 3.1 角色定义（首期可实现简化版 RBAC）

| 角色 | 权限摘要 |
| ---- | -------- |
| 系统管理员 | 用户与角色、系统配置、审计查询、备份策略 |
| 知识库管理员 | 创建/删除知识库、导入/删除文档、重建索引、查看解析失败任务 |
| 知识贡献者 | 上传文档、查看自己上传任务状态（可选） |
| 普通用户 | 问答、查看与自己会话相关的引用（不可改库） |
| 只读审计员 | 只读审计日志与统计（可选） |

### 3.2 权限原则

- **最小权限**：上传、删库、改模型配置等高危操作需管理员角色。  
- **对象级**：文档与向量、会话元数据按「知识库」隔离；API 校验 `kb_id` / `tenant_id`（预留）。  
- **密钥**：LLM / Embedding API Key **仅存服务端**（环境变量或密钥管理服务），前端仅通过已鉴权会话间接调用后端，不在浏览器长期保存明文 Key（若 demo 阶段允许本地存储，须在文档中标注**仅限开发环境**）。

### 3.3 认证方式（分阶段）

| 阶段 | 建议 |
| ---- | ---- |
| MVP | 简单登录（JWT 或 Session）+ 固定测试账号 |
| 试点/生产 | 对接企业 IdP：**OIDC / SAML 2.0**（如 Azure AD、Keycloak），支持组映射到角色 |

---

## 4. 系统架构设计

### 4.1 逻辑架构（推荐演进方向）

```
┌─────────────────────────────────────────────────────────────────────────┐
│  接入层：HTTPS / 反向代理 (Nginx) / WAF(可选) / 限流                         │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
┌─────────────────────────────────────────────────────────────────────────┐
│  应用层：Web UI (Vue3)  │  BFF/API (FastAPI)  │  管理 API（可与主 API 分前缀） │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
        ┌─────────────┬───────────────┼───────────────┬─────────────┐
        ▼             ▼               ▼               ▼             ▼
   文档解析任务    向量化任务      检索服务         LLM 网关        配置与密钥
   (Worker/Celery  (Worker)      (混合+可选Rerank)  (流式/SSE)     (DB+Secret)
    或 RQ 等)      
        │             │               │               │             │
        └─────────────┴───────────────┴───────────────┴─────────────┘
                                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  数据层：向量库 │ 关系库(元数据/用户/审计) │ 对象存储(MinIO/S3/本地) │ 缓存(Redis 可选) │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 三栏 UI 与模块对应（与实现保持一致即可）

| 区域 | 职责 |
| ---- | ---- |
| 一侧：检索与引用 | 展示命中 chunk、分数、元数据、展开原文、复制引用 |
| 中间：对话 | 输入、流式输出、多轮上下文、会话列表 |
| 另一侧：配置与知识库 | 模型连接、分块参数、知识库与文档管理、任务进度 |

> 具体左右与产品设计一致即可，需求层面强调「配置 / 引用 / 对话」三区功能完备。

### 4.3 分层说明

| 层级 | 职责 | 技术选型（可替换，需写明适配点） |
| ---- | ---- | -------------------------------- |
| 表现层 | 三栏布局、Markdown、SSE | Vue3 + TS + Element Plus |
| 应用层 | 路由、鉴权、会话、限流、OpenAPI | FastAPI |
| 领域服务 | 解析、分块、索引、检索、Rerank、Prompt、LLM | LangChain / LlamaIndex / 自研薄封装 |
| 数据层 | 向量、元数据、文件 blob | Chroma / pgvector / Milvus（演进）；PostgreSQL；MinIO |

---

## 5. 核心功能需求

### 5.1 知识库与文档管理

| 功能 | 说明 | 优先级 |
| ---- | ---- | ------ |
| 多知识库 | 不同业务线/项目独立库，独立向量集合与权限 | P1 |
| 上传 | 拖拽/批量；单文件大小上限可配置；病毒扫描为可选集成点 | P0 |
| 支持格式 | PDF、DOCX、TXT、MD、XLSX | P0 |
| 解析策略 | 按标题层级 / 固定长度 / 语义分块（可配置）；表格、列表尽量保留结构 | P0 |
| 分块参数 | `chunk_size`、`chunk_overlap` 可配置并持久化 | P0 |
| 嵌入模型 | 可切换；记录「库-模型」绑定，避免混用不同维度向量 | P0 |
| 异步流水线 | 大文件解析与向量化走任务队列，可重试、可查看失败原因 | P1 |
| 删除与重建 | 按文档删除向量；全库重建；增量更新（变更检测）为 P2 | P0/P2 |
| 元数据 | 文件名、路径、上传人、时间、页码、章节、hash、版本号 | P0 |

### 5.2 问答与检索

| 功能 | 说明 | 优先级 |
| ---- | ---- | ------ |
| 混合检索 | 向量 + BM25（或关键词稀疏向量），可配置权重 | P0 |
| Rerank | Cross-Encoder 等精排，可开关 | P1 |
| 查询理解 | 同义词扩展、轻量改写为可选项；需记录是否启用 | P2 |
| 多轮对话 | `session_id` 维护历史；上下文窗口与摘要策略可配置 | P0 |
| 流式输出 | SSE；首 token 时间、总时长可记录 | P0 |
| 引用与拒答 | 无足够依据时拒答模板；禁止编造未出现在上下文中的条款编号 | P0 |
| 调试检索 | `/api/search` 仅管理员或 debug 角色可用 | P1 |

### 5.3 配置管理

| 配置项 | 说明 |
| ------ | ---- |
| 模型提供商 | OpenAI 兼容 / Azure / 本地 Ollama / 国内云厂商 API |
| 端点与模型名 | Base URL、Deployment、模型 ID |
| 生成参数 | temperature、max_tokens、top_p |
| 检索参数 | top_k、score 阈值、混合检索权重、rerank top_n |
| 存储与队列 | 向量库类型、Redis、对象存储连接串（服务端环境变量） |

配置持久化：**环境变量 + 加密字段（数据库）** 组合；禁止将生产密钥写入前端仓库。

---

## 6. 非功能需求（企业落地关键）

### 6.1 性能与容量（可按试点规模调整）

| 指标 | 建议目标（试点） | 说明 |
| ---- | ---------------- | ---- |
| 问答端到端 P95 | ≤ 8～15s（视模型与硬件） | 需区分「首字延迟」与「全文完成」 |
| 检索 P95 | ≤ 500ms～2s | 不含 LLM；向量规模上升需压测 |
| 并发 | 20～50 在线用户（无队列） | 超出则排队或限流 |
| 单文档大小 | 默认 ≤ 50MB（可配） | 超大 PDF 建议异步与分页解析 |
| 向量规模 | 百万级 chunk 需评估向量库与硬件 | 文档中写明压测计划 |

### 6.2 可用性与部署

- **健康检查**：`/health`（进程）、`/ready`（依赖：DB、向量库、队列）。  
- **无状态 API**：会话状态入 DB 或 Redis；水平扩展时依赖共享存储。  
- **滚动更新**：Docker 镜像版本号；数据库迁移脚本（Alembic 等）。  
- **时区与多语言**：时间戳统一 UTC；界面中文为主，错误码表可中英。

### 6.3 安全

| 类别 | 要求 |
| ---- | ---- |
| 传输 | 全站 HTTPS；HSTS（生产） |
| 鉴权 | 所有业务 API 需登录态；管理接口单独前缀与更严角色 |
| 输入 | 上传类型白名单、大小限制；防路径穿越；可选 ClamAV |
| 输出 | Markdown 渲染防 XSS；外链 `rel="noopener noreferrer"` |
| 密钥 | KMS / 环境变量；日志脱敏（不打印完整 Key、PII） |
| CORS | 白名单域名，禁止 `*` 配 credentials |

### 6.4 审计与合规

| 事件 | 记录字段示例 |
| ---- | -------------- |
| 登录/登出 | user_id、时间、IP、User-Agent、结果 |
| 知识库变更 | 操作者、kb_id、动作、对象 id |
| 文档上传/删除 | 文档 id、hash、结果 |
| 问答（可配置采样或仅管理员库） | session_id、问题摘要 hash、引用 doc 列表、模型名（**不存完整答案**或按策略脱敏） |

保留周期、归档与 GDPR/个人信息场景需在部署指南中由企业法务确认（本需求只要求**可配置保留策略与导出接口预留**）。

### 6.5 可观测性

- **结构化日志**：JSON 日志，含 `trace_id`、`user_id`（hash）、耗时。  
- **指标**：QPS、4xx/5xx、检索延迟、LLM 调用失败率、队列积压长度、embedding 耗时。  
- **追踪**：OpenTelemetry 接入为 P2，试点可先日志 + metrics。  
- **告警**：连续 5xx、队列堆积、磁盘用量超阈值。

### 6.6 备份与恢复

- 关系库：每日全量 + binlog/WAL（若用 PostgreSQL）。  
- 向量库：按厂商备份策略；或与「可从原文重建」策略一致（接受重建时间）。  
- 对象存储：版本控制或周期快照。  
- **恢复演练**：每季度一次记录在运维手册。

---

## 7. API 与契约（补充）

### 7.1 通用约定

- **版本前缀**：`/api/v1/...`，破坏性变更递增主版本。  
- **错误体**（示例）：

```json
{
  "error": {
    "code": "KB_NOT_FOUND",
    "message": "知识库不存在",
    "trace_id": "a1b2c3d4"
  }
}
```

- **分页**：`limit` + `cursor` 或 `page` + `page_size`（文档中固定一种）。  
- **幂等**：上传同一 `content_hash` 可返回「已存在」而非重复索引（可选）。

### 7.2 核心接口（在原有基础上扩展）

| 接口 | 方法 | 说明 |
| ---- | ---- | ---- |
| `/api/v1/auth/login` | POST | MVP：用户名密码；演进：OIDC |
| `/api/v1/auth/logout` | POST | 吊销会话/刷新令牌 |
| `/api/v1/knowledge-bases` | GET/POST | 知识库列表与创建 |
| `/api/v1/knowledge-bases/{kb_id}/documents` | GET/POST | 列表与上传（返回 `job_id`） |
| `/api/v1/jobs/{job_id}` | GET | 解析/向量化进度与错误详情 |
| `/api/v1/chat` | POST | 问答；`stream: true` 时 SSE |
| `/api/v1/sessions` | GET/DELETE | 会话列表与删除 |
| `/api/v1/search` | POST | 调试检索（受权） |
| `/api/v1/admin/audit-logs` | GET | 审计查询（管理员） |
| `/health`、`/ready` | GET | 运维探针 |

### 7.3 流式事件类型（SSE）

建议统一 `event` 类型，便于前端与排障：

- `citation`：引用列表  
- `answer_delta`：增量文本  
- `error`：可恢复或不可恢复错误码  
- `done`：结束（可带 `usage` 统计）  

---

## 8. 数据模型要点

### 8.1 实体关系（逻辑）

- **User** 1—N **Session**  
- **KnowledgeBase** 1—N **Document** 1—N **Chunk**（向量 id 与 chunk 映射表）  
- **IngestionJob** 关联 Document 与状态机：`pending → parsing → embedding → done | failed`  
- **AuditLog** 追加写，禁止应用层物理删除（合规域外另议）

### 8.2 向量与元数据

每个 chunk 元数据至少包含：`kb_id`、`document_id`、`chunk_index`、`source_uri`、`page`（如有）、`heading_path`、`content_hash`、`embedding_model_id`。

---

## 9. RAG 质量与评测（落地必做）

### 9.1 基线评测集

- 准备 **30～100 条** 企业真实或脱敏问答对（含「应拒答」样本）。  
- 指标：**引用命中率**、**答案相关性**（人工或 LLM-as-judge）、**拒答正确率**、**P95 延迟**。

### 9.2 回归与 CI

- 解析与分块：单元测试 + 黄金样例文件快照。  
- 检索：固定种子与固定 embedding stub 的可重复测试。  
- 可选：夜间任务跑评测集，结果存 artifact。

---

## 10. 技术栈与选型说明

在原文档技术栈基础上补充：

| 组件 | 选型建议 | 说明 |
| ---- | -------- | ---- |
| 关系库 | PostgreSQL | 用户、权限、任务、审计、会话 |
| 队列 | Redis + RQ / Celery / ARQ | 异步入库与重试 |
| 向量库 | Chroma（学习）→ pgvector / Milvus（规模） | 迁移成本在架构文档中单列一章 |
| 网关 | Nginx / Traefik | TLS、限流、静态前端 |
| 密钥 | Vault / K8s Secret / 环境变量 | 按企业规范选择 |

---

## 11. 部署拓扑（参考）

### 11.1 单机 Docker Compose（学习 + 小部门试点）

- `api` + `worker` + `postgres` + `redis` + `minio` + `nginx`（可选）  
- 向量库内嵌或独立容器

### 11.2 生产演进（简述）

- API 与 Worker 分离扩缩容；PostgreSQL 主从；对象存储用企业 S3；向量库集群。  
- LLM 内网推理服务（vLLM / TGI）与 RAG 服务同 VPC。

---

## 12. 实施计划与里程碑

### 12.1 阶段划分（建议）

| 阶段 | 周期（参考） | 目标 |
| ---- | ------------ | ---- |
| **L0 教学 MVP** | 1～2 周 | 单库、同步入库、基础向量检索、无鉴权或单用户、Chroma + SQLite |
| **L1 可试点** | 2～3 周 | 登录与 RBAC 雏形、异步任务、PostgreSQL、引用与拒答策略、结构化日志 |
| **L2 可部门落地** | 2～4 周 | 混合检索、Rerank、限流、健康检查、备份 Runbook、最小评测集 |
| **L3 生产加固** | 持续 | OIDC、完整审计、密钥管理、高可用、OpenTelemetry、向量库迁移 |

### 12.2 验收标准（在原文档基础上增强）

| 维度 | 标准 |
| ---- | ---- |
| 功能 | 多库、上传、异步任务可查、流式问答、引用展示、拒答场景可演示 |
| 安全 | 生产配置下无浏览器明文 Key；HTTPS；RBAC 生效 |
| 质量 | 评测集基线达标（由项目启动时书面确认具体数值） |
| 运维 | `/ready` 正常；备份与恢复步骤有文档且演练记录 |
| 文档 | OpenAPI、部署架构图、数据流与保留策略说明 |

---

## 13. 风险与依赖

| 风险 | 缓解 |
| ---- | ---- |
| 扫描版 PDF 无文本层 | OCR 路线或要求提供可检索 PDF |
| 表格问答效果差 | 表格结构化解析、专用 prompt、或 Tabular RAG 扩展 |
| 模型供应商不稳定 | 多厂商路由、超时重试、降级文案 |
| 向量与元数据不一致 | 任务幂等、重建工具、定期一致性校验任务 |

---

## 14. 学习路径建议（与工程实践对齐）

1. RAG 概念：检索 → 上下文组装 → 生成 → 引用  
2. 文档解析与分块对召回的影响（用同一问题对比实验）  
3. 向量检索与 BM25 的差异与融合  
4. Prompt 与拒答、安全边界  
5. 异步任务、失败重试与可观测性  
6. 鉴权、审计与部署安全基线  
7. 评测集驱动迭代，而非主观「感觉变好」

---

## 15. 附录：参考链接

- [LangChain 文档](https://python.langchain.com/)  
- [FastAPI](https://fastapi.tiangolo.com/)  
- [Chroma](https://www.trychroma.com/)  
- [Ollama](https://ollama.com/)  
- [OpenTelemetry](https://opentelemetry.io/)  

---

## 16. 与 v1 文档的主要变更摘要

- 增加**范围/非目标**、**角色权限与认证演进**、**异步入库任务**与 **Job 状态**。  
- 补充**非功能需求**：性能、安全、审计、备份、可观测性、健康检查。  
- 统一 **API 版本、错误体、SSE 事件** 约定；扩展运维与管理接口。  
- 明确**数据模型**、**评测与 CI**、**部署拓扑演进**与**风险表**。  
- 保留三栏 UI 与 RAG 流水线核心描述，并与企业落地项对齐为分阶段里程碑。

---

*本文档可作为「自学项目规格说明」与「内部试点需求说明」共用；实施时请将指标类章节中的区间值按实际硬件与模型 SLA 书面固化。*
